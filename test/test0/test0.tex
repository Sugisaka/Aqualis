\documentclass[a4paper,fleqn]{ltjsarticle}
\usepackage{amsmath}
\oddsidemargin=-0.4mm
\topmargin=4.6mm
\headheight=0mm
\headsep=0mm
\footskip=15mm
\textwidth=160mm
\textheight=237mm
\topsep=6pt
\parindent=0mm
\unitlength=1.00mm
\begin{document}
{\Large test0}
\section{structures}
\section{subroutines}
\section{global variables}
\begin{itemize}
\item char $t^{(1)}$
\item file $f^{(0)}$
\item char $t^{(1)}$
\item file $f^{(0)}$
\item complex $z$
\item double $y$
\item int $x$
\item Cache variables (integer): \(i_m (m = 1 \cdots [i; i^{(1)}; i^{(2)}; ... ])\)
\item Cache variables (complex): \(z_m (m = 1)\)
\item Cache array (integer,1d): \(\dot{i}_m (m = 1)\)
\end{itemize}
\section{main code}
\begin{align}
x \leftarrow 1
\end{align}
\begin{align}
y \leftarrow 3.141592653589793
\end{align}
\begin{align}
z \leftarrow uj
\end{align}
for $i^{(3)}=0\cdots 10-1$\\
  \begin{align}
  i \leftarrow i^{(1)}+i+i^{(2)}
  \end{align}
end\\
for $i^{(3)}=0\cdots 10-1$\\
  \begin{align}
  i \leftarrow i^{(1)}+i+i^{(2)}
  \end{align}
end\\
for $i^{(3)}=0\cdots 10-1$\\
  \begin{align}
  i \leftarrow i^{(1)}+i+i^{(2)}
  \end{align}
end\\
\begin{align}
i^{(1)} \leftarrow 1
\end{align}
if \left[i^{(1)} < i^{(2)} \cap i^{(2)} < i^{(3)}\right] \cup i^{(3)} < 1
  \begin{align}
  i^{(1)} \leftarrow 0
  \end{align}
endif
\begin{align}
i^{(2)} \leftarrow 2
\end{align}
\begin{align}
i^{(3)} \leftarrow 3
\end{align}
print, i^{(1)}
print, "aaa"
print, i^{(1)},i^{(2)},i^{(3)}
print, i^{(1)},aaa,i^{(2)},bbb,i^{(3)}
f^{(0)} = open text file(t^{(1)},"w");
\begin{align}
z^{(1)} \leftarrow 1+uj 2
\end{align}
Write(text): \(f^{(0)} \leftarrow \mathrm{Re}\left(z^{(1)}\right),\mathrm{Im}\left(z^{(1)}\right)\)
close(f^{(0)});
f^{(0)} = open text file(t^{(1)},"r");
read(f^{(0)},"()",iostat=i^{(1)}) z^{(1)}
print, z^{(1)}
close(f^{(0)});
$\dot{i}^{(1)} \in \mathbb{Z}^{10}$\\
\begin{align}
\dot{i}^{(1)}_{0} \leftarrow 0
\end{align}
$\dot{i}^{(1)}$: deallocate\\
\end{document}
