module setting
    // directory for auto backup of fsx files
    let backupdir = [@"C:\home\backup"]
    // directory for version list data for auto version numbering
    let listdir = @"C:\home\backup"
    // set "true" if auto backup is enabled
    let isCopySourceDir = true
